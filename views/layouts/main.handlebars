<html>

<head>
  <meta charset="utf-8" />
  <meta name="description" content="" />
  <meta http-equiv="Content-Security-Policy" content="upgrade-insecure-requests" />
  <title>Mixtape Generator</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>

<body class="bg-stone-900">
  <nav class="relative bg-gradient-to-r from-emerald-900 via-stone-900 to-stone-900 p-6">
    <div class="items-center flex justify-between">
      <h1 class="text-xl mdlrg:text-2xl tracking-wider text-white">
        Mixtape Generator
      </h1>
      <div class="flex items-center">
        <form class="form new-media-form">
        <div class="form-group">
          <input id="media-title" name="media-title" type="text" class="rounded-l-2xl w-32 mdlrg:w-44 pl-3" placeholder="Search" />
          <input type="submit" value="submit"
            class="bg-stone-200 rounded-r-2xl cursor-pointer active:bg-stone-400 active:text-stone-200 px-2 mr-9" />
        </div>
        </form>
        <div class="hidden mdlrg:flex items-center font-mono text-stone-400 space-x-4">
          {{#if logged_in}}
          <a href="/profile" class="active:text-stone-600 hover:text-white">Favorites</a>
          <a href="/logout"
            class="text-emerald-700 hover:bg-emerald-700 hover:text-white drop-shadow-button text-shadow-sm rounded border-2 py-1 px-4 border-emerald-700">Logout</a>
          {{ else }}
          <a href="/login"
            class="text-emerald-700 hover:bg-emerald-700 hover:text-white drop-shadow-button text-shadow-sm rounded border-2 py-1 px-4 border-emerald-700">Sign
            Up/Login</a>
          {{/if}}
        </div>
      </div>
      <button id="menu-btn" class="block hamburger mdlrg:hidden focus:outline-none">
        <span class="hamburger-top"></span>
        <span class="hamburger-middle"></span>
        <span class="hamburger-bottom"></span>
      </button>
    </div>
    <div class="mdlrg:hidden">
      <div id="menu"
        class="absolute hidden flex flex-col items-center self-end py-8 mt-6 text-white space-y-6 font-bold bg-stone-600 sm:w-auto sm:self-center left-6 right-6 drop-shadow-m">
        <a href="#">Favorites</a>
        <a href="#">Sign Up/Login</a>
      </div>
    </div>
  </nav>
  {{{ body }}}
</body>
{{#if logged_in}}
<script src="/js/profile.js"></script>
<script src="./js/logout.js"></script>
{{/if}}

</html>